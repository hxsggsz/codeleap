import styled from 'styled-components';

export const Styledheader = styled.header`
  width: 100vw;
  padding: 2.7rem;
  color: var(--white);
  background: var(--blue);
  position: fixed;
  top: 0;
  left: 0;
  z-index: 9;
`;